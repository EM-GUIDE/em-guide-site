---
interface Props {
  label: string;
  url: string;
  newPage?: boolean | null;
}

const { label, url, newPage } = Astro.props;
---

<a
  class="bg-gray-medium px-4 py-2.5 rounded-10 font-display inline-block mt-8"
  href={url}
  target={newPage ? "_blank" : "_self"}>{label}</a
>
